rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES - Allow all writes for mock data creation
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
