export const metadata = {
  title: 'Petitions - 3arida',
  description: 'Browse and sign petitions to make change happen in Morocco',
};

export default function PetitionsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
